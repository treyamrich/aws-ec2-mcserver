apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: mc-server-controller-pod-manager
  namespace: {{ .Values.namespace }}
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["create", "delete", "get", "list"]
