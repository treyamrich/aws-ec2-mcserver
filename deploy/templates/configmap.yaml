apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.mcServer.configMap.name }}
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $val := .Values.mcServer.configMap.data }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
